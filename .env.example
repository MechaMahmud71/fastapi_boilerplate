APP_ENV=development
LOG_LEVEL=INFO

DATABASE_URL=postgresql+asyncpg://faruk:faruk@localhost:5434/todo
JWT_SECRET=change-me
JWT_ALGORITHM=HS256
JWT_EXPIRE_TIME=60

# Comma-separated exact origins. Defaults to * in development.
CORS_ORIGINS=
CORS_ALLOW_CREDENTIALS=true

# Interactive docs: on in development, off in production unless forced.
# ENABLE_DOCS=true
